# Optional; LLM is disabled unless explicitly configured.
OPENROUTER_API_KEY=
# Optional; required only for the `screen` LLM fit-screening command.
AITUNNEL_API_KEY=
APPLYPILOT_DATA_DIR=./private/data
APPLYPILOT_PROFILE=./private/config/profile.toml

